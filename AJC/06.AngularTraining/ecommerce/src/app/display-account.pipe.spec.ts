import { DisplayAccountPipe } from './display-account.pipe';

describe('DisplayAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
